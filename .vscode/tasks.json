{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Swift Build (SDK=iOS)",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "swift build --swift-sdk arm64-apple-ios"
        }
    ]
}